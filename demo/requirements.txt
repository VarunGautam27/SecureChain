# A curated, exact-count manifest, the PyPI counterpart to demo/package.json:
# 2 Critical, 1 High, 2 Medium (real CVEs, verified via GitHub Advisory/NVD),
# and 5 Unverified (no catalogued CVE). Of the 5 Unverified, dlib genuinely
# resolves to High via the static-scan path (a real, well-known computer
# vision library with a genuine custom setup.py install step compiling
# native code - PyPI install-hook detection added specifically to catch
# this, since PyPI has no declarative "scripts" field like npm's
# package.json) and the remaining 4 resolve to Safe.
#
# No Low-severity (0.1-3.9) or 2nd-High/3rd-Critical entry is included: no
# real, version-verified CVE at that tier was available without a
# GITHUB_TOKEN in this environment (NVD's keyword-only fallback produced
# unreliable matches when checked - e.g. a totally unrelated CVE from 2004),
# and unverified data was deliberately excluded rather than used.
#
# `six` was removed from this file: live (non-cached) scans of it show a
# known NVD keyword-collision false positive (Critical) unrelated to
# anything in this project's own code - removed to avoid confusion rather
# than kept with a caveat.
pyyaml==5.3.1
pillow==8.4.0
werkzeug==2.2.2
urllib3==1.26.16
jinja2==3.1.2
certifi==2024.2.2
idna==3.6
charset-normalizer==3.3.2
dlib==20.0.1
iniconfig==2.0.0
